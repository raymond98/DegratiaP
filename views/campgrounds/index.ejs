<% include ../partials/header %>

<div class="row">
    <!--<div class="col-md-3">-->
            <!--<div class="list-group">-->
            <!--    <li class="list-group-item active">Home Page</li>-->
            <!--    <li class="list-group-item">Clothing</li>-->
            <!--    <li class="list-group-item">Home Equipments</li>-->
            <!--    <li class="list-group-item">Bags</li>-->
            <!--    <li class="list-group-item">Shoes</li>-->
            <!--</div>-->
        <!--</div>-->
        <div class = "container">
            <div class = "col-md-12">
                <header class="jumbotron">
                </header>
                <div class="row text-center" style = "display:flex; flex-wrap: wrap;">
                    <% campgrounds.forEach(function(campground){%>
                    <div class = "col-md-4 col-sm-6">
                        <div class = "thumbnail">
                            <img src = "<%= campground.image %>">
                            <div class = "caption">
                                 <h4><a href="/<%= campground._id %>"><%= campground.name %></a></h4>
                            </div>
                            <p>
                                <a href="/<%= campground._id %>" class="btn btn-info" class="pull-left"> More Info</a>
                                <a href="/<%= campground._id %>" class="btn btn-default" class="pull-right"> <i>R <%= campground.price %></i></a>
                            </p>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

<% include ../partials/footer %>